---
title: Accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
---

import { AccordionDemo, AccordionVariants } from '../../../components/examples/AccordionDemo';
import Preview from '../../../components/Preview.astro';

## Preview

<Preview>
  <AccordionDemo client:load />
</Preview>

## Installation

```bash
npx handwritten-ui add accordion
```

## Usage

```tsx
import {
  Accordion,
  AccordionItem,
  AccordionTrigger,
  AccordionContent,
} from '@/components/ui/accordion';

<Accordion type="single" defaultValue="item-1">
  <AccordionItem value="item-1">
    <AccordionTrigger>Is it accessible?</AccordionTrigger>
    <AccordionContent>
      Yes. It adheres to the WAI-ARIA design pattern.
    </AccordionContent>
  </AccordionItem>
</Accordion>
```

## Variants

<AccordionVariants client:load />

## Props

### Accordion

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `type` | `'single' \| 'multiple'` | `'single'` | Determines whether one or multiple items can be opened at the same time. |
| `defaultValue` | `string \| string[]` | — | The value of the item(s) to expand by default. |

### AccordionItem

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | (required) | A unique value for the item. |
